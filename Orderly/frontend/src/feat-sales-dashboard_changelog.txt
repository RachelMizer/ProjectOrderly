CHANGELOG — feat-sales-summary-dashboard
Date: 2026-04-11
Author: Rachel Mizer
====================================================


PR DESCRIPTION
--------------
Implements the admin sales summary dashboard (UX5.4.1–5.4.6), nested
under the Reports section of the admin UI. Includes a reports hub
landing page, a fully styled sales summary dashboard with stat tiles,
a sortable and filterable data table, and a live bar chart powered by
Recharts. Sales data is served from generated CSV seed files via a
new backend API endpoint. Filtering by year and month is supported,
with the current year loaded by default.


CHANGES
-------

frontend/src/pages/Admin/reports.jsx
  - Replaced placeholder <h1> with a full Reports hub page.
  - Added submenu bar with disabled OPEN FILE, EXPORT, and PRINT
    action buttons (title="Pending further development").
  - Added rpt-hub-grid with a Sales Summary card linking to
    /admin/reports/sales. Additional report types can be added
    as cards in future sprints.

frontend/src/pages/Admin/AdminSalesDashboard.jsx  [NEW FILE]
  - New page component mounted at /admin/reports/sales.
  - Submenu bar with search input, year dropdown, month dropdown,
    and disabled OPEN FILE / EXPORT / PRINT action buttons.
  - Period label displayed above stat tiles: shows
    "Year-to-Date Sales for {year}", a specific month name, or
    "All Years" depending on active filter state.
  - Two stat tiles: Total Revenue (UX5.4.2) and Units Sold (UX5.4.3).
    All currency values formatted with commas via toLocaleString.
  - Bar chart (Recharts BarChart) below stat tiles: shows daily
    revenue bars when a month is selected, monthly revenue bars
    when viewing a year or all months. Tooltip displays both
    revenue and units sold. Brand blue (#33638B) bar color.
  - Sortable data table (UX5.4.4): columns are Product, Variant,
    Unit Price, Units Sold, Total Revenue. Sortable on all five
    columns with ▲/▼/⇅ indicators consistent with other admin
    tables. Defaults to Units Sold descending.
  - Search bar filters table by product name or variant.
  - Year dropdown defaults to current calendar year on load.
    Changing year resets month selection and refetches.
    Month dropdown shows only months available in the selected year.
  - Loading state, error state, and empty states implemented
    (UX5.4.5).
  - All styles use Admin.css classes; no inline styles (UX5.4.6).

frontend/src/Admin.js
  - Added import for AdminSalesDashboard.
  - Added route: <Route path="/reports/sales" element={<AdminSalesDashboard />} />.

frontend/src/api/adminReports.js  [NEW FILE]
  - fetchSalesSummary({ year, month }) — GET
    /api/v1/admin/reports/sales-summary with optional ?year= and
    ?month= query parameters. Follows same auth header pattern as
    adminInventory.js.

frontend/src/Admin.css
  - Added Reports hub styles: rpt-hub-grid, rpt-hub-card,
    rpt-hub-card__title, rpt-hub-card__desc.
  - Added Sales Dashboard styles: rpt-period-label, rpt-stat-row,
    rpt-stat-card, rpt-stat-label, rpt-stat-value, rpt-empty,
    rpt-loading, rpt-month-select.

backend/inventory/api/admin_reports_view.py  [NEW FILE]
  - AdminSalesSummaryView (GET /api/v1/admin/reports/sales-summary).
  - Reads *_sales.csv files from accounts/management/commands/.
  - Parses MONTHLY SUMMARY section for table data (product, variant,
    unit_price, units_sold, revenue per product-variant).
  - Parses DAILY SALES section for chart data: daily aggregates when
    a month is selected, monthly aggregates when viewing a year or all.
  - Accepts ?year= and ?month= query parameters for filtering.
    ?month= takes precedence and reads a single file; ?year= filters
    the file list; neither reads all files and aggregates.
  - Returns: total_revenue, order_count, available_years,
    available_months (filtered to selected year), chart_data, products.
  - Permission: IsAuthenticated + IsBusinessUser.

backend/inventory/api/admin_urls.py
  - Added route: path("reports/sales-summary", AdminSalesSummaryView).

backend/accounts/management/commands/generate_sales_csv.py  [NEW FILE]
  - Django management command: python manage.py generate_sales_csv
  - Generates one CSV per month with three sections:
      1. DAILY SALES — one row per product-variant per day
      2. MONTHLY SUMMARY — aggregated totals per product-variant
      3. MONTH TOTALS — grand total units and revenue
  - Products and prices match seed_data.py variants exactly.
  - Day-of-week multipliers applied (Saturday highest, Sunday lowest).
  - Special date overrides: Christmas Eve (+50%), Christmas Day (-80%),
    New Year's Day (-75%), Valentine's Day (+20%), etc.
  - Seasonal adjustments: PSL on menu Aug–Nov (ramp up Oct, taper Nov),
    off menu Dec–Apr. Cold Brew peaks in summer, slower in winter.
  - Output is deterministic via seeded RNG (--seed flag, default 42).
  - Accepts --seed argument for alternate data sets.

backend/accounts/management/commands/  [CSV FILES GENERATED]
  - 08-2025_sales.csv — August 2025
  - 09-2025_sales.csv — September 2025
  - 10-2025_sales.csv — October 2025
  - 11-2025_sales.csv — November 2025
  - 12-2025_sales.csv — December 2025
  - 01-2026_sales.csv — January 2026
  - 02-2026_sales.csv — February 2026
  - 03-2026_sales.csv — March 2026
  - 04-2026_sales.csv — April 2026 (to-date: Apr 1–11)


KNOWN ISSUES / FUTURE WORK
---------------------------
1. EXPORT / OPEN FILE / PRINT
   Action buttons in the submenu bar are present but disabled
   (title="Pending further development"). Export will eventually
   download the filtered CSV data. Chart export (PNG) is planned
   as a separate action using the Recharts SVG ref.

2. BAR CHART — PENDING RECHARTS INSTALL
   The backend chart_data endpoint is complete and returns daily or
   monthly aggregates depending on the active filter. The frontend
   Recharts BarChart component is built and wired to chart_data.
   Requires: npm install recharts (run in /frontend).

3. APRIL TO-DATE CUTOFF
   The April 2026 CSV is hardcoded to Apr 1–11 in the generator.
   As the month progresses, the file will need to be regenerated
   with an updated cutoff or replaced by a live orders query.

4. LIVE DATA
   All sales data is currently CSV-based seed data. The long-term
   architecture should replace or supplement the CSV read with a
   live query against the Order model once order history is
   populated in production.
