SPRINT CONTRIBUTION SUMMARY — Rachel Mizer
Project: Orderly — Quick Sip Cafe Business Management System
Course: CSC 289, Wake Tech, Spring 2026
Role: Frontend Developer
====================================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPRINT 1 — Planning & Documentation  (Jan–Feb 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Created README.md for the repository (Jan 31)
- Created Django_Scripts.md — reference doc for common Django management
  commands and dev environment setup (Feb 7)
- Created feature_list.md — initial frontend feature list draft (Feb 7)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPRINT 2 — Authentication System  (Mar 14–15, 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Branch: feat-authentication (merged to main)

Built the core frontend authentication system from scratch, along with
significant backend account model cleanup and consolidation.

Frontend (new files):
  frontend/src/api/auth.js
    - JWT auth service: login, logout, register, token refresh, profile
      get/update, password change, password reset request, and reset confirm.
  frontend/src/pages/Login.jsx
    - Login form with JWT token storage and redirect on success.
  frontend/src/pages/Register.jsx
    - Registration form with full field validation and error display.
  frontend/src/pages/UserProfile.jsx
    - View and edit profile: name, email, phone, address fields.
  frontend/src/pages/ResetPassword.jsx
    - Password reset form (accepts UID + token from email link).
  frontend/src/pages/ResetPasswordSuccess.jsx
    - Confirmation screen shown after successful password reset.
  frontend/src/pages/ResetRequest.jsx
    - "Forgot password" form — submits email to trigger reset email.

Backend (accounts app — major overhaul):
  - Consolidated and cleaned up accounts migrations (removed redundant/broken
    migration files, rebuilt clean migration history).
  - accounts/models.py: restructured UserProfile, CustomerProfile, and
    BusinessUserProfile models.
  - accounts/api/serializers.py: registration, login, profile, and password
    serializers — added field validation and normalization.
  - accounts/api/views.py: registration, login, profile CRUD, password change,
    password reset request and confirm endpoints.
  - accounts/api/urls.py: wired all auth endpoints.
  - orderly/settings.py: JWT configuration, CORS, media settings.
  - orderly/urls.py: connected accounts API routes.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPRINT 3 — Storefront & Product Browsing  (Mar 26–28, 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Branch: feat-product-browsing (merged to main)

Built the customer-facing storefront UI including product grid, filtering,
hover tooltips, and individual product detail pages.

frontend/src/pages/StoreFront.jsx
  - Product grid layout fetching from catalog API.
  - Category filter bar — filters products by category on click.
  - Hover tooltip showing product name and base price on card hover.
  - Responsive grid with Quick Sip Cafe branding (QSlogo.png).

frontend/src/components/ProductCard.jsx  [NEW FILE]
  - Reusable card component: product image, name, base price, hover state.

frontend/src/pages/ProductPage.jsx  [NEW FILE]
  - Individual product detail page: product image, name, description,
    variant selector, base price display.
  - Routed via /product/:id.

frontend/src/App.css
  - Full storefront and product page styling: grid, card, tooltip, layout.

frontend/src/App.js
  - Added routes for StoreFront and ProductPage.
  - Replaced placeholder logo/favicon with Quick Sip Cafe assets.

Assets added:
  - public/img/QSlogo.png — Quick Sip Cafe logo
  - public/favicon.ico — updated favicon
  - src/assets/fonts/ — Munson font family (Roman, Bold, Italic, BoldItalic)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPRINT 3 — Item Customization  (Mar 30, 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Branch: item-customization-pg (merged into feat-shopping-cart-pg)

frontend/src/pages/ProductPage.jsx
  - Added variant selection (radio buttons replacing dropdown).
  - Added modifier group display with quantity/toggle controls.
  - Price updates dynamically as variant and modifiers are selected.

frontend/src/App.css
  - Variant radio button styles, modifier group layout, pricing display.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPRINT 3 — Shopping Cart  (Mar 31 – Apr 3, 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Branch: feat-shopping-cart-pg (merged to main)

Implemented the full shopping cart page and wired order totals end-to-end,
including backend tax and modifier pricing fixes.

frontend/src/pages/Cart.jsx
  - Load cart on mount from orders API; display all items with variant,
    modifier group/name, and size label.
  - Quantity controls (increment/decrement) per line item.
  - Delete individual item; empty entire cart button.
  - Edit item — navigates back to ProductPage in edit mode (pre-selects
    variant and modifiers; deletes old item before re-adding).
  - Item total = base price + modifier price adjustments.
  - Cart totals: subtotal, Wake County 7.25% tax, grand total.
  - Empty-cart state with header preserved.

frontend/src/components/ProductCard.jsx
  - Fixed localStorage auth key (was 'access', corrected to 'accessToken').
  - Replaced variant dropdown with radio buttons.
  - Hidden size selector when only one / "Standard" variant exists.
  - Display product description when present.

frontend/src/pages/ProductPage.jsx
  - Edit-mode via URL params: pre-selects variant and modifiers from
    existing cart item, deletes old item before re-adding (no duplicates).
  - Fixed localStorage auth key.
  - Added modifier quantity field to POST payload.

frontend/src/pages/Profile.jsx
  - Replaced all inline styles with CSS classes.
  - Phone number formatted with dashes on load and after save.

frontend/src/pages/Login.jsx
  - Removed duplicate setLoggedIn/navigate calls on successful login.

frontend/src/App.js
  - Added cart nav badge with live item count.

frontend/src/App.css
  - Cart page layout and styling, profile page classes, variant radio styles.

frontend/src/api/auth.js
  - Refactored and trimmed; unified token handling.

Backend:
  orders/services.py
    - Implemented Wake County 7.25% tax calculation.
    - Included modifier price_adjustment_charged in subtotal recalculation.
    - Extended modifier prefetch to include group for serializer access.
  orders/serializers.py
    - Added productId to DraftOrderItemSerializer.
    - Added groupName to OrderItemModifierSerializer.
    - Added itemTotal as SerializerMethodField (base + modifier charges).
  catalog/api/serializers.py
    - Exposed groupName on modifier options.
  accounts/api/serializers.py
    - Strip dashes/spaces/parens from phone before validation so formatted
      input (e.g. 919-555-1212) is accepted and normalized to digits.

Post-merge fixes (Apr 1–3):
  - Added product descriptions to seed data; updated StoreFront cards and
    ProductPage to display descriptions (Apr 1).
  - Ordering system styling pass: OrderHistory.jsx, OrderDetail.jsx,
    Checkout.jsx layout and visual polish; extracted orders.js API service;
    refactored orders/views.py (Apr 3).
  - Backend cart service bug fixes: subtotal/tax edge cases (Apr 3).


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPRINT 4 — Admin Dashboard UI  (Apr 8–10, 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Branch: feat-admin-dashboard-ui (merged as PR #48)

Built the entire admin panel architecture from scratch — router, layout,
auth, sidebar navigation, and all base page scaffolding.

frontend/src/Admin.js  [NEW FILE]
  - Central admin router: handles all /admin/* routes.
  - Admin-specific authentication guard (BusinessUser role required).
  - Sidebar navigation with links to Dashboard, Inventory, Products,
    Orders, Reports, and Account pages.
  - AdminLogin redirect for unauthenticated access attempts.

frontend/src/Admin.css  [NEW FILE]
  - Full admin stylesheet: sidebar layout, nav items, active states,
    top bar, content area, table base styles, form base styles.
  - Renner font applied throughout admin UI.
  - Brand blue (#33638B) color system.

frontend/src/pages/Admin/AdminLogin.jsx  [NEW FILE]
  - Standalone admin login page, separate from customer login.
  - Redirects to /admin/dashboard on successful BusinessUser auth.

frontend/src/pages/Admin/dashboard.jsx
  - Admin dashboard landing page with placeholder stat tiles.

frontend/src/pages/Admin/admin-acct.jsx
  - Admin account settings page.

Placeholder pages scaffolded (to be built out in subsequent sprints):
  - inventory.jsx, orders.jsx, prod-cat.jsx, reports.jsx

Assets:
  - src/assets/fonts/Renner.ttf — added to project for admin UI
  - public/o_favicon.ico — custom favicon for admin
  - Updated public/logo192.png and logo512.png to Quick Sip branding

Backend:
  - accounts/api/serializers.py: exposed is_business_user on user serializer
    so frontend can conditionally render admin nav elements.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPRINT 4/5 — Product Management Page  (Apr 9–10, 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Branch: feat-prod-mgmt-pg (merged as PR #49)

Implemented the full admin product catalog management page — create, read,
update, delete for products, variants, and suppliers.

frontend/src/pages/Admin/AdminProductsPage.jsx
  - Full product catalog table: paginated list of all products with
    variant count, category, availability status, and image.
  - Inline search/filter by product name.
  - Sortable columns (name, category, status) with ▲/▼/⇅ indicators.
  - Delete product with confirmation modal.
  - Edit and Create buttons route to AdminProductFormPage.

frontend/src/pages/Admin/AdminProductFormPage.jsx  [NEW FILE]
  - Create and edit form for products and their variants (396 lines).
  - Fields: name, description, category, availability toggle, image upload.
  - Variant sub-section: add/remove variants, set price and stock per variant.
  - Supplier association field.
  - Validates required fields before submit; shows inline errors.
  - PUT (edit) and POST (create) to admin catalog API.

frontend/src/pages/Admin/AdminSupplierFormPage.jsx  [NEW FILE]
  - Create and edit form for suppliers (124 lines).
  - Fields: supplier name, contact name, email, phone, address.
  - POST/PUT to admin supplier API.

frontend/src/Admin.css
  - Added product form styles: form layout, variant sub-rows, image
    preview, modal overlay and dialog, status badge variants.
  - 318 lines of new CSS added in this feature.

Backend adjustments:
  - catalog/api/admin_serializers.py: added fields required by the
    new form pages (image URL, variant stock, supplier linkage).
  - catalog/api/admin_views.py: updated to support image upload via
    multipart/form-data.
  - accounts/api/permissions.py and exceptions.py: minor corrections
    for consistency with RBAC enforcement across admin endpoints.
  - accounts/management/commands/seed_data.py: added missing product
    entries to align with CSV sales data.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPRINT 5 — Sales Summary Dashboard  (Apr 11–13, 2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Branch: feat-sales-sum-dash (in progress — not yet merged)

Implemented the admin sales summary dashboard under the Reports section,
including a new backend reporting API and a CSV-based sales data seed system.

frontend/src/pages/Admin/reports.jsx
  - Replaced placeholder with a full Reports hub landing page.
  - Submenu bar with disabled OPEN FILE, EXPORT, and PRINT buttons
    (pending further development).
  - rpt-hub-grid with a Sales Summary card linking to /admin/reports/sales.
    Additional report types can be added as cards in future sprints.

frontend/src/pages/Admin/AdminSalesDashboard.jsx  [NEW FILE]
  - Full sales summary dashboard mounted at /admin/reports/sales.
  - Submenu bar: search input, year dropdown, month dropdown, action buttons.
  - Period label: "Year-to-Date Sales for {year}", specific month name,
    or "All Years" depending on active filter.
  - Stat tiles: Total Revenue and Units Sold; all values formatted with
    toLocaleString for comma-separated display.
  - Bar chart (Recharts BarChart): daily revenue bars when a month is
    selected; monthly revenue bars when viewing a year or all months.
    Tooltip shows both revenue and units sold. Brand blue (#33638B) bars.
  - Sortable data table: columns are Product, Variant, Unit Price,
    Units Sold, Total Revenue. Sortable on all five columns with ▲/▼/⇅
    indicators consistent with other admin tables. Defaults to Units Sold
    descending.
  - Search bar filters table rows by product name or variant.
  - Year dropdown defaults to current calendar year on load; changing year
    resets month selection and refetches data.
  - Month dropdown shows only months available in the selected year.
  - Loading, error, and empty states implemented.
  - All styles use Admin.css classes; no inline styles.

frontend/src/pages/Admin/AdminProductPerformance.jsx  [NEW FILE]
  - Product performance detail view component.

frontend/src/api/adminReports.js  [NEW FILE]
  - fetchSalesSummary({ year, month }) — GET
    /api/v1/admin/reports/sales-summary with optional ?year= and ?month=
    query params. Follows same auth header pattern as adminInventory.js.

frontend/src/Admin.css
  - Added Reports hub styles: rpt-hub-grid, rpt-hub-card,
    rpt-hub-card__title, rpt-hub-card__desc.
  - Added Sales Dashboard styles: rpt-period-label, rpt-stat-row,
    rpt-stat-card, rpt-stat-label, rpt-stat-value, rpt-empty,
    rpt-loading, rpt-month-select.

frontend/src/Admin.js
  - Added import for AdminSalesDashboard.
  - Added route: /reports/sales → <AdminSalesDashboard />.

frontend/src/utils/recentViews.js  [NEW FILE]
  - Utility for tracking recently viewed admin pages.

Backend:
  inventory/api/admin_reports_view.py  [NEW FILE]
    - AdminSalesSummaryView (GET /api/v1/admin/reports/sales-summary).
    - Reads *_sales.csv files from accounts/management/commands/.
    - Parses MONTHLY SUMMARY section for table data (product, variant,
      unit_price, units_sold, revenue per product-variant).
    - Parses DAILY SALES section for chart data: daily aggregates when
      a month is selected, monthly aggregates when viewing a year or all.
    - Accepts ?year= and ?month= query parameters for filtering.
    - Returns: total_revenue, order_count, available_years,
      available_months (filtered to selected year), chart_data, products.
    - Permission: IsAuthenticated + IsBusinessUser.
  inventory/api/admin_urls.py
    - Added route: path("reports/sales-summary", AdminSalesSummaryView).
  inventory/api/admin_serializers.py
    - Serializers supporting the sales summary response shape.

  accounts/management/commands/generate_sales_csv.py  [NEW FILE]
    - Django management command: python manage.py generate_sales_csv
    - Generates one CSV per month with three sections:
        1. DAILY SALES — one row per product-variant per day
        2. MONTHLY SUMMARY — aggregated totals per product-variant
        3. MONTH TOTALS — grand total units and revenue
    - Products and prices match seed_data.py variants exactly.
    - Day-of-week multipliers applied (Saturday highest, Sunday lowest).
    - Special date overrides: Christmas Eve (+50%), Christmas Day (-80%),
      New Year's Day (-75%), Valentine's Day (+20%), etc.
    - Seasonal adjustments: PSL on menu Aug–Nov (ramp up Oct, taper Nov),
      off menu Dec–Apr. Cold Brew peaks in summer, slower in winter.
    - Output is deterministic via seeded RNG (--seed flag, default 42).
    - April cutoff automatically set to date.today() at generation time.

  CSV seed files generated (Aug 2025 – Apr 2026 to-date):
    08-2025_sales.csv, 09-2025_sales.csv, 10-2025_sales.csv,
    11-2025_sales.csv, 12-2025_sales.csv, 01-2026_sales.csv,
    02-2026_sales.csv, 03-2026_sales.csv, 04-2026_sales.csv

KNOWN ISSUES / FUTURE WORK
---------------------------
1. EXPORT / OPEN FILE / PRINT
   Action buttons present but disabled. Export will eventually download
   filtered CSV. Chart PNG export via Recharts SVG ref is planned.

2. BAR CHART — REQUIRES RECHARTS INSTALL
   Backend chart_data endpoint complete. Frontend BarChart component built
   and wired. Requires: npm install recharts (in /frontend).

3. LIVE DATA
   All sales data is CSV-based seed data. Long-term architecture should
   replace or supplement with a live query against the Order model once
   order history is populated in production.
