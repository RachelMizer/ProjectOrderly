feat-checkout-pg — Session Change Log
======================================
Date: 2026-04-02

MERGE CONFLICT RESOLUTIONS
---------------------------
- App.js: Resolved 3 merge conflicts between HEAD and feat-shopping-cart-pg
    - Imports: kept both OrderDetails and CartPage
    - Nav: merged Order History link (HEAD) with active cart badge/link (feat-shopping-cart-pg)
    - Routes/footer: kept all routes, added /cart route, used updated footer text

- Login.jsx: Resolved 1 merge conflict between HEAD and feat-shopping-cart-pg
    - Kept HEAD version (submit button wrapped in <div>)

NEW FEATURES
------------
- frontend/src/api/orders.js
    - Added submitOrder(orderId, paymentData)
      PATCH /api/v1/orders/{orderId}/submit

- frontend/src/pages/Checkout.jsx (new file)
    - Read-only order review: items, modifiers, quantities, subtotal, tax, grand total
    - Simulated payment fields: name, phone, address, city, state, zip
    - Payment type selector: Credit Card, Cash, Other
    - Conditional fields: card last 4 (Credit Card) / details field (Other)
    - Empty cart guard with link back to cart
    - Dispatches cart-updated event and navigates to order detail on success


- frontend/src/App.js
    - Imported Checkout component
    - Added /checkout route

- frontend/src/App.css
    - Styled active "Go to Checkout" button
    - Added full checkout page styles (.checkout-pg, .checkout-item, .checkout-totals,
      .checkout-fieldset, .checkout-field, .checkout-actions, .submit-order-btn, etc.)

BUG FIXES
---------
- frontend/src/components/ProductCard.jsx
- frontend/src/pages/ProductPage.jsx
    - Fixed: stockQuantity === null (non-stock-tracked items) was being treated as
      out-of-stock because Number(null) === 0; Add to Cart button was never rendered
    - Fix: null stockQuantity now treated as always available

GUEST CART SUPPORT
------------------
- backend/orders/services.py
    - Added get_or_create_guest_draft_order(guest_email)
    - Added order_item_belongs_to_guest(order_item, guest_email)
    - Added get_order_for_guest(order_id, guest_email)

- backend/orders/views.py
    - DraftOrderView: changed to AllowAny; guests pass guestEmail in request body
    - DraftOrderItemCreateView: changed to AllowAny; guests pass guestEmail in request body
    - DraftOrderItemUpdateView: changed to AllowAny; guests pass guestEmail in request body
    - OrderDetailView: changed to AllowAny; guests pass guestEmail as query param

- frontend/src/api/orders.js
    - Added getGuestCartEmail(): generates and persists a guest_<uuid>@cart.local
      token in localStorage to identify guest carts across sessions

- frontend/src/components/ProductCard.jsx
- frontend/src/pages/ProductPage.jsx
- frontend/src/pages/Cart.jsx
    - All cart operations now pass guestEmail when user is not logged in

- frontend/src/pages/Checkout.jsx
    - Redirects to /login if user is not authenticated (guests can shop but
      must log in to check out)

- frontend/src/App.js
    - fetchCartCount now supports guests: reads guestCartEmail from localStorage
      and passes it to the draft/detail endpoints
    - Cart badge now updates for guest users on cart-updated events
