feat-checkout-pg — Image Support Change Log
============================================
Date: 2026-04-03

BACKEND
-------
- catalog/models.py
    - Added image = ImageField(upload_to='products/', null=True, blank=True) to Product model

- catalog/api/serializers.py
    - Updated get_imageUrl() to return full absolute URL when an image exists
    - Falls back to None when no image is set

- catalog/api/views.py
    - Passed request context to ProductSerializer so imageUrl builds absolute URLs

- orderly/settings.py
    - Added MEDIA_URL = "/media/"
    - Added MEDIA_ROOT = BASE_DIR / "media"

- orderly/urls.py
    - Added static() media file serving for development

- Migration required:
    - python manage.py makemigrations catalog
    - python manage.py migrate

- Dependency required:
    - pip install Pillow

FRONTEND
--------
- frontend/src/components/ProductCard.jsx
- frontend/src/pages/ProductPage.jsx
    - Replaced static placeholder div with conditional: shows <img> if imageUrl set,
      placeholder div if not

- frontend/src/App.css
    - Added object-fit: cover and object-position: center to img.img
    - Images fill the 200x200 square and crop from center regardless of aspect ratio

NOTES
-----
- Images uploaded via Django admin at /admin until dashboard upload UI is built
- Uploaded images stored in backend/media/products/
